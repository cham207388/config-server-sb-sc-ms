#server.port=0
server.port=8080
spring.datasource.url=jdbc:mysql://localhost:3306/department_db
spring.datasource.username=root
spring.datasource.password=720668Aa#
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

#eureka
eureka.client.register-with-eureka=true
eureka.client.service-url.default-zone=http://localhost:8761
#eureka.instance.instance-id=${spring.application.name}:${server.port}
#
#actuator
management.endpoints.web.exposure.include=*
eureka.instance.instance-id=${spring.application.name}:${server.port}
#eureka.instance.instance-id=${spring.application.name}:${spring.application.instance-id:${random.int(12,7999)}}


spring.boot.message=Department service message updated

# Trace every action
management.tracing.sampling.probability=1.0
#level
logging.level.org.springframework.web.servlet.DispatcherServlet=DEBUG
logging.pattern.level=%5p [${spring.zipkin.service.name:${spring.application.name:}},%X{traceId:-},%X{spanId:-}]
spring.jackson.serialization.indent_output = true

management.zipkin.tracing.endpoint=127.0.0.1:9411